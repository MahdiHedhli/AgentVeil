# Payload map moved

The canonical, evidence-labeled map is [PAYLOAD_MAP.md](PAYLOAD_MAP.md). This
compatibility page prevents the earlier pre-implementation map from being
mistaken for current coverage.
